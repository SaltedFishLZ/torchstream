#/bin/bash

# NOTE: Please tyep the command manually !!!
# Once you use script, Virutalenv will use global interpreter
# I don't know why.

# DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
# VENV_PATH="venv_video"

# # create a bare environment
# echo "Creating Virtual Environment..."
# virtualenv ${VENV_PATH} --python python3 -v

virtualenv venv_video --python python3 -v
